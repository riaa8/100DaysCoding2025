package Package;
public class Day20 {
    public static void main(String[] args) { 
        int angka = 20;
        String teks = String.valueOf(angka);

        System.out.println("Int asli bisa dihitung: " + (angka + 10));
        System.out.println("String tidak bisa dihitung, hanya digabung teks: " + (teks + 10));
    }
    
}
